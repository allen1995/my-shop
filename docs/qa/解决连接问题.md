# 解决 Mini-Test 连接问题

## 🔍 问题诊断

**现象**：
- ✅ 微信开发者工具已启动
- ✅ 端口 39411 正在监听
- ✅ TCP 连接测试通过
- ❌ WebSocket 连接失败（Handshake status 200 OK，但连接失败）

**原因**：微信开发者工具中的**自动化服务端口未启用**

## ✅ 解决步骤

### 步骤 1: 打开微信开发者工具

确保微信开发者工具窗口已打开，并且**项目窗口已打开**（能看到小程序预览界面）。

### 步骤 2: 启用服务端口（关键步骤）

1. **点击设置按钮**
   - 在微信开发者工具右上角，找到**齿轮图标**（设置按钮）
   - 或者使用菜单：**设置** -> **安全设置**

2. **启用服务端口**
   - 在"安全设置"窗口中
   - **勾选**：**开启服务端口**
   - 确认端口号显示为：**39411**
   - 如果端口号不同，请记录实际端口号

3. **保存设置**
   - 关闭设置窗口
   - 等待 5-10 秒让服务完全启动

### 步骤 3: 验证连接

运行测试脚本验证连接：

```bash
cd docs/qa
python test-websocket-connection.py
```

如果连接成功，应该看到：
```
✓ WebSocket 连接成功！
✓ 消息发送成功
✓ 收到响应
```

### 步骤 4: 使用 MCP 工具连接

连接成功后，使用 MCP 工具连接：

```python
mcp_mini-test_minium_connect(wsEndpoint="ws://127.0.0.1:39411")
```

## 📋 完整检查清单

在执行连接前，请确认：

- [ ] 微信开发者工具窗口已打开
- [ ] **项目窗口已打开**（能看到小程序预览界面，不能只是工具启动）
- [ ] 设置 -> 安全设置 -> **开启服务端口** 已勾选 ✅
- [ ] 端口号确认为 **39411**（或记录实际端口号）
- [ ] 项目已编译且无错误
- [ ] 等待 5-10 秒让服务完全启动
- [ ] 运行 `python test-websocket-connection.py` 测试连接

## 🎯 常见问题

### Q1: 找不到"开启服务端口"选项

**解决**：
- 确保使用的是最新版本的微信开发者工具
- 某些版本可能在不同位置，尝试：**设置** -> **编辑器设置** -> **安全**

### Q2: 端口号不是 39411

**解决**：
- 记录实际显示的端口号
- 在连接时使用实际端口号：
  ```python
  mcp_mini-test_minium_connect(wsEndpoint=f"ws://127.0.0.1:{实际端口号}")
  ```

### Q3: 启用后仍然连接失败

**解决**：
1. 关闭微信开发者工具
2. 重新使用 CLI 启动：
   ```bash
   D:\Tencent\微信web开发者工具\cli.bat auto --project D:\code\my-shop-cursor\frontend\unpackage\dist\dev\mp-weixin --auto-port 39411
   ```
3. 等待项目完全加载
4. 在工具中手动启用服务端口
5. 重新测试连接

### Q4: 连接成功但无法操作小程序

**解决**：
- 确保小程序已编译成功
- 确保没有编译错误
- 尝试刷新小程序（在开发者工具中点击刷新按钮）

## 🚀 连接成功后的操作

连接成功后，可以执行以下操作：

1. **导航到页面**
   ```python
   mcp_mini-test_minium_navigate_to(url="/pages/index/index")
   ```

2. **获取页面元素**
   ```python
   mcp_mini-test_minium_get_element(selector=".class-name")
   ```

3. **点击元素**
   ```python
   mcp_mini-test_minium_element_tap(selector="button")
   ```

4. **输入文本**
   ```python
   mcp_mini-test_minium_element_input(selector="input", value="测试文本")
   ```

5. **截图**
   ```python
   mcp_mini-test_minium_screenshot(path="screenshot.png")
   ```

## 📝 下一步

完成连接后，我们可以：
1. 执行全功能测试
2. 测试各个页面功能
3. 验证用户流程
4. 生成测试报告

请按照上述步骤启用服务端口，然后告诉我，我们可以继续执行全功能测试。

